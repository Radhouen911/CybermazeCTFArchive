// Configuration for the arcade theme
export const config = {
  // CTFd API base URL
  apiBaseUrl: "/api/v1",

  // Development server settings
  devServer: {
    port: 3000,
    ctfdProxyUrl: "http://localhost:8000",
  },
};
